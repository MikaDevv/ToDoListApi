spring:
  application:
    name: tasks
  profiles:
    active: dev
    
  datasource:
    url: jdbc:mysql://localhost:3306/tasksdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      jakarta:
        persistence:
          validation:
            mode: auto